// Minimal stub kept so parseLeadText.ts continues to compile.
// Returns null when the input is not a numeric word we recognize.
export function parseGermanNumberWord(_input: string): number | null {
  return null;
}